import React from 'react';
import './Summary.css';

const Summary = ({ answers }) => {
    return (
        <div className="summary">
            <h1>Summary</h1>
            {/* Logic to display the summary based on answers */}
        </div>
    );
};

export default Summary;
